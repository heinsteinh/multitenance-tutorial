# Step 10: Complete System

This final step integrates all components into a production-ready multi-tenant gRPC service.

## Complete Architecture

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                              gRPC Clients                                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ Web Client  │  │ Mobile App  │  │   CLI Tool  │  │   SDK       │         │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘         │
└─────────┼────────────────┼────────────────┼────────────────┼─────────────────┘
          │                │                │                │
          └────────────────┴───────┬────────┴────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│                           Load Balancer / Gateway                            │
└──────────────────────────────────┬───────────────────────────────────────────┘
                                   │
          ┌────────────────────────┼────────────────────────┐
          ▼                        ▼                        ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   gRPC Server   │    │   gRPC Server   │    │   gRPC Server   │
│    Instance 1   │    │    Instance 2   │    │    Instance N   │
└────────┬────────┘    └────────┬────────┘    └────────┬────────┘
         │                      │                      │
         └──────────────────────┼──────────────────────┘
                                │
┌───────────────────────────────┴──────────────────────────────────────────────┐
│                           Interceptor Chain                                  │
│  ┌───────────┐ → ┌───────────┐ → ┌───────────┐ → ┌───────────┐               │
│  │  Logging  │   │   Auth    │   │  Tenant   │   │  Metrics  │               │
│  └───────────┘   └───────────┘   └───────────┘   └───────────┘               │
└──────────────────────────────────┬───────────────────────────────────────────┘
                                   │
┌──────────────────────────────────┴───────────────────────────────────────────┐
│                              Handlers                                        │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐               │
│  │  TenantHandler  │  │   UserHandler   │  │  OrderHandler   │               │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘               │
└───────────┼────────────────────┼────────────────────┼────────────────────────┘
            │                    │                    │
┌───────────┴────────────────────┴────────────────────┴────────────────────────┐
│                              Services                                        │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐               │
│  │  TenantService  │  │   UserService   │  │  OrderService   │               │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘               │
│           │                    │                    │                        │
│           └────────────────────┼────────────────────┘                        │
│                                ▼                                             │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │                      Authorization Service                              │ │
│  └─────────────────────────────┬───────────────────────────────────────────┘ │
└────────────────────────────────┼─────────────────────────────────────────────┘
                                 │
┌────────────────────────────────┴─────────────────────────────────────────────┐
│                              Repositories                                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ TenantRepo   │  │  UserRepo    │  │  OrderRepo   │  │PermissionRepo│      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
└─────────┼─────────────────┼─────────────────┼─────────────────┼──────────────┘
          │                 │                 │                 │
          └─────────────────┴────────┬────────┴─────────────────┘
                                     │
┌────────────────────────────────────┴─────────────────────────────────────────┐
│                           Tenant Manager                                     │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │                      Connection Pool Manager                            │ │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐                     │ │
│  │  │ Pool 1  │  │ Pool 2  │  │ Pool 3  │  │ Pool N  │                     │ │
│  │  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘                     │ │
│  └───────┼────────────┼────────────┼────────────┼──────────────────────────┘ │
└──────────┼────────────┼────────────┼────────────┼────────────────────────────┘
           │            │            │            │
           ▼            ▼            ▼            ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            SQLite Databases                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ System   │  │ Tenant A │  │ Tenant B │  │ Tenant C │  │ Tenant N │       │
│  │   DB     │  │    DB    │  │    DB    │  │    DB    │  │    DB    │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
└─────────────────────────────────────────────────────────────────────────────┘
```

## Project Structure

```
step10-complete-system/
├── CMakeLists.txt
├── vcpkg.json
├── config/
│   ├── config.yaml           # Application configuration
│   └── logging.yaml          # Logging configuration
├── proto/
│   ├── common.proto
│   ├── tenant.proto
│   ├── user.proto
│   └── order.proto
├── include/
│   ├── config/
│   │   └── config.hpp        # Configuration loading
│   ├── db/                   # From Step 02
│   ├── pool/                 # From Step 03
│   ├── repository/           # From Step 04
│   ├── tenant/               # From Step 05
│   ├── services/             # From Step 07
│   ├── interceptors/         # From Step 08
│   ├── auth/                 # From Step 09
│   └── server/
│       └── grpc_server.hpp   # Server orchestration
├── src/
│   ├── main.cpp              # Entry point
│   ├── server.cpp            # Server implementation
│   └── health_check.cpp      # Health check endpoint
├── tests/
│   ├── integration/
│   │   ├── tenant_test.cpp
│   │   ├── user_test.cpp
│   │   └── auth_test.cpp
│   └── unit/
│       └── ...
├── scripts/
│   ├── migrate.sh            # Database migrations
│   └── seed.sh               # Test data seeding
└── docker/
    ├── Dockerfile
    └── docker-compose.yml
```

## Configuration

```yaml
# config/config.yaml
server:
  address: "0.0.0.0:50051"
  max_threads: 4
  
database:
  system_db: "./data/system.db"
  tenant_directory: "./data/tenants/"
  pool:
    min_connections: 2
    max_connections: 10
    
auth:
  jwt_secret: "${JWT_SECRET}"
  token_expiry_hours: 24
  
logging:
  level: "info"
  format: "[%Y-%m-%d %H:%M:%S.%e] [%^%l%$] [%n] %v"
  
metrics:
  enabled: true
  port: 9090
```

## Server Implementation

```cpp
class MultiTenantServer {
public:
    MultiTenantServer(const Config& config)
        : config_(config)
        , tenant_manager_(config.database.system_db, 
                         config.database.tenant_directory)
        , jwt_validator_(config.auth.jwt_secret)
    {
        setup_services();
        setup_interceptors();
    }
    
    void run() {
        ServerBuilder builder;
        
        // Configure server
        builder.AddListeningPort(config_.server.address,
                                grpc::InsecureServerCredentials());
        
        // Register services
        builder.RegisterService(&tenant_handler_);
        builder.RegisterService(&user_handler_);
        builder.RegisterService(&order_handler_);
        builder.RegisterService(&health_service_);
        
        // Add interceptors
        builder.experimental().SetInterceptorCreators(
            std::move(interceptor_factories_));
        
        // Build and start
        server_ = builder.BuildAndStart();
        spdlog::info("Server listening on {}", config_.server.address);
        
        // Wait for shutdown signal
        server_->Wait();
    }
    
    void shutdown() {
        spdlog::info("Shutting down server...");
        server_->Shutdown();
        tenant_manager_.close_all_pools();
    }

private:
    void setup_services() {
        // Initialize services
        tenant_service_ = std::make_unique<TenantService>(tenant_manager_);
        user_service_ = std::make_unique<UserService>(tenant_manager_, auth_service_);
        order_service_ = std::make_unique<OrderService>(tenant_manager_);
        
        // Initialize handlers
        tenant_handler_ = TenantHandler(*tenant_service_);
        user_handler_ = UserHandler(*user_service_);
        order_handler_ = OrderHandler(*order_service_);
    }
    
    void setup_interceptors() {
        interceptor_factories_.push_back(
            std::make_unique<InterceptorFactory>(
                tenant_manager_,
                jwt_validator_,
                auth_service_
            )
        );
    }
};
```

## Health Check Service

```cpp
class HealthServiceImpl : public grpc::health::v1::Health::Service {
public:
    Status Check(ServerContext* context,
                 const HealthCheckRequest* request,
                 HealthCheckResponse* response) override {
        
        // Check database connectivity
        if (!tenant_manager_.get_system_pool().is_healthy()) {
            response->set_status(HealthCheckResponse::NOT_SERVING);
            return Status::OK;
        }
        
        response->set_status(HealthCheckResponse::SERVING);
        return Status::OK;
    }
};
```

## Docker Deployment

```dockerfile
# Dockerfile
FROM ubuntu:22.04 AS builder

RUN apt-get update && apt-get install -y \
    build-essential cmake git curl zip unzip tar pkg-config

# Install vcpkg
RUN git clone https://github.com/Microsoft/vcpkg.git /vcpkg
RUN /vcpkg/bootstrap-vcpkg.sh

# Copy source
COPY . /app
WORKDIR /app

# Build
RUN cmake -B build \
    -DCMAKE_TOOLCHAIN_FILE=/vcpkg/scripts/buildsystems/vcpkg.cmake \
    -DCMAKE_BUILD_TYPE=Release
RUN cmake --build build --parallel

# Runtime image
FROM ubuntu:22.04

COPY --from=builder /app/build/multitenant-server /usr/local/bin/
COPY config/ /etc/multitenant/

EXPOSE 50051
VOLUME ["/data"]

CMD ["multitenant-server", "--config", "/etc/multitenant/config.yaml"]
```

## Docker Compose

```yaml
version: '3.8'

services:
  server:
    build: .
    ports:
      - "50051:50051"
      - "9090:9090"  # Metrics
    volumes:
      - ./data:/data
    environment:
      - JWT_SECRET=${JWT_SECRET}
    healthcheck:
      test: ["CMD", "grpc_health_probe", "-addr=:50051"]
      interval: 10s
      timeout: 5s
      retries: 3
```

## Running the Complete System

```bash
# Build
cmake --preset=release
cmake --build build

# Run with config
./build/multitenant-server --config config/config.yaml

# Or with Docker
docker-compose up -d
```

## Testing

```bash
# Run unit tests
./build/tests/unit_tests

# Run integration tests
./build/tests/integration_tests

# Client smoke test
./build/client --server localhost:50051 --test
```

## Congratulations! 🎉

You've completed the tutorial! You now have a production-ready multi-tenant gRPC service with:

- ✅ SQLite database with RAII wrappers
- ✅ Thread-safe connection pooling
- ✅ Repository pattern for data access
- ✅ Database-per-tenant isolation
- ✅ gRPC services with protobuf
- ✅ Interceptors for cross-cutting concerns
- ✅ Role-based access control
- ✅ JWT authentication
- ✅ Complete service layer architecture
